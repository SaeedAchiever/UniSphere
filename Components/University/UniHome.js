import { View, Text, ScrollView } from 'react-native'

import styles from './UniHomeStyle'

const UniHome = () => {
  return (
    <ScrollView>
     <View>
      <Text>UniHome</Text>
     </View>
    </ScrollView>
  )
}

export default UniHome